const React = require("react");

var About = (props) => {
    return (
        <div>
            <h2>ABOUT</h2>
            <p>This is the about page</p>
        </div>
    );
};

module.exports = About;
